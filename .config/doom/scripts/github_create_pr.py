#!/usr/bin/env python3

def main(script_args=None):
    #args = script_args if (script_args != None) else parser.parse_args()
    print("WIP")
    #TODO: also detect if a PR was ALREADY made so we dont make a redundant one

if __name__ == "__main__":
    main()
